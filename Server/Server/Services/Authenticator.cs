namespace Server.Services
{
    /// <summary>
    /// Provides various authentication services.
    /// </summary>
    public class Authenticator
    {
        /// <summary>
        /// Returns true if a connection is successfully authenticated.
        /// </summary>
        /// <returns></returns>
        public static bool AuthenticateConnection(string authToken, string key)
        {
            return false;
        }
    }
}
